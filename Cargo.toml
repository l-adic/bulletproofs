[package]
name = "bulletproofs"
version = "0.1.0"
edition = "2024"

[dependencies]
ark-ec = {version = "0.5.0", features = ["parallel"]}
ark-ff = {version = "0.5.0", features = ["parallel"]}
ark-secp256k1 = "0.5.0"
proptest = "1.7.0"
rand = "0.8"

[dev-dependencies]
ark-std = "0.5.0"